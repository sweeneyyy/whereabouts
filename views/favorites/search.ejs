<h2>Search For Inspiration and Save Your Favorites</h2>

<div class="container">
  <form method="POST" action="/favorites/search">
    <input name="searchTag" placeholder="search inspiration">
    <button type="submit">search</button>
  </form>
</div>



<% results.forEach(function(item){ %>
  <div class="container">
    <div class="row">
      <div class="col-sm-3">
        <form method="POST" action="/favorites">
          <input id="POST-url" hidden type="url" name="url" value="<%= item %>">
          <img src="<%= item %>" class="img-responsive img-rounded">
          <button class="btn btn-xs btn-default center-block" type="submit">
            <span class="glyphicon glyphicon-star"><span id="add"> Add to Favorites</span></span>
          </button>
        </form>
      </div>
      <div class="col-sm-3">
        <form method="POST" action="/favorites">
          <input id="POST-url" hidden type="url" name="url" value="<%= item %>">
          <img src="<%= item %>" class="img-responsive img-rounded">
          <button class="btn btn-xs btn-default center-block" type="submit">
            <span class="glyphicon glyphicon-star"><span id="add"> Add to Favorites</span></span>
          </button>
        </form>
      </div>
      <div class="col-sm-3">
        <form method="POST" action="/favorites">
          <input id="POST-url" hidden type="url" name="url" value="<%= item %>">
          <img src="<%= item %>" class="img-responsive img-rounded">
          <button class="btn btn-xs btn-default center-block" type="submit">
            <span class="glyphicon glyphicon-star"><span id="add"> Add to Favorites</span></span>
          </button>
        </form>
      </div>
      <div class="col-sm-3">
        <form method="POST" action="/favorites">
          <input id="POST-url" hidden type="url" name="url" value="<%= item %>">
          <img src="<%= item %>" class="img-responsive img-rounded">
          <button class="btn btn-xs btn-default center-block" type="submit">
            <span class="glyphicon glyphicon-star"><span id="add"> Add to Favorites</span></span>
          </button>
        </form>
      </div>
    </div>
  </div>
<% }); %>